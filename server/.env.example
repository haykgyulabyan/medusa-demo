# Supabase credentials — copy this file to `server/.env` and fill in.
# Easiest: run `vercel env pull server/.env` from the repo root (the project is
# already linked in .vercel/project.json) and it will populate these for you.

SUPABASE_URL=
SUPABASE_ANON_KEY=
